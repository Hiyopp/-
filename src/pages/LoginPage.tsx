import styled from "styled-components";

const HomeWrapper = styled.div`
  width: 100%;
  height: 100%;

  display: flex;
  justify-content: center;
  align-items: center;
`;

export function LoginPage() {
    return (
        <HomeWrapper>login</HomeWrapper>
    );
}